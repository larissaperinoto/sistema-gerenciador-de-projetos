import { Router } from "express";

const route = Router();

route.get("/projetos", () => {});
route.post("/projetos", () => {});
route.put("/projetos/:id", () => {});
route.delete("/projetos/:id", () => {});

export default route;
